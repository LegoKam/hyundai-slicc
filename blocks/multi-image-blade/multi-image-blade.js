export default async function decorate(block) {
  // Each row is a card with [image-cell, text-cell]
  // The CSS handles the overlay positioning via absolute positioning
  // No DOM restructuring needed — the two-cell structure works as-is
}
